#Define the check_palindrome function here def check_palindrome(str_list): palindrome_list = [] for word in str_list: if word.lower() == word.lower()[::-1]: palindrome_list.append(word) return palindrome_list #Sample main section. #Do not remove the below portion of code. if __name__=='__main__': count=int(input()) input_strings=[] for i in range(count): input_strings.append(input()) output=check_palindrome(input_strings) if len(output)!=0: for word in output: print(word) else: print('No palindrome found')